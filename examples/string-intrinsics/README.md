# string-intrinsics

Exercises deterministic `String` intrinsics: `length`, `charAt`, `equals`, and `isEmpty`.

```sh
dist/javan build examples/string-intrinsics
examples/string-intrinsics/.javan/bin/string-intrinsics
```
