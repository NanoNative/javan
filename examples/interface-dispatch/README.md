# interface-dispatch

Exercises deterministic `invokeinterface` lowering when closed-world analysis finds exactly one concrete implementation.

```sh
dist/javan build examples/interface-dispatch
examples/interface-dispatch/.javan/bin/interface-dispatch
```
