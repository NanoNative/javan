# Object Fields Example

Builds a small multi-class Java project with object allocation, constructor field writes, field reads, exact final-class instance calls, and a static helper that accepts an object reference.

```sh
./build-example.sh
```
