# long-basic

Exercises scalar `long` constants, parameters, locals, arithmetic, returns, and `System.out.println(long)`.

```sh
dist/javan build examples/long-basic
examples/long-basic/.javan/bin/long-basic
```
