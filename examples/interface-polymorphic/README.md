# Polymorphic interface dispatch

Acceptance project for closed-world interface dispatch with multiple implementations.

```sh
../../dist/javan build . --output interface-polymorphic
.javan/bin/interface-polymorphic
```
