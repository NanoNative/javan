# library-dependency

Builds a tiny dependency jar and then builds an app against that jar with `javan`.

```sh
src/test/resources/projects/acceptance/library-dependency/build-example.sh
```

Expected output:

```text
42
```
