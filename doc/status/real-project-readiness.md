# Real Project Readiness

Status summary:

| Project | Status | Current evidence | Missing release gate |
| --- | --- | --- | --- |
| TypeMap | Partial | `typemap-pair` builds natively against the pinned Maven artifact and is exercised by required CI acceptance plus focused CLI integration. | Broader TypeMap dependency graph and quieter unreachable-dependency diagnostics. |
| Nano metrics helper | Partial | `nano-metric` builds natively against the pinned Maven artifact and is exercised by required CI acceptance plus focused CLI integration. | Broader Nano dependency graph and quieter unreachable-dependency diagnostics. |
| Nano duration helper | Partial | `nano-duration` builds natively against the pinned Maven artifact and is exercised by required CI acceptance plus focused CLI integration. | Broader Nano helper surface and quieter unreachable-dependency diagnostics. |
| Nano HTTP service | In progress | A stripped real Nano `/hello` service route without `DevConsoleService` now also gets past the inherited default-interface regression on `TypeListI.addR(...)` / `TypeListI.addAllR(...)`, the direct `String.replace(CharSequence,CharSequence)` seam used deeper in TypeMap/Nano helpers, the focused compiler/runtime/accounting `ProcessHandle.current()` / `ProcessHandle.pid()` slice, `Class.isInstance(Object)` / `Class.cast(Object)` / `Class.isEnum()` / `Class.isAssignableFrom(Class)`, the current `Class.getCanonicalName()` alias-on-runtime-class-name slice, the end-to-end `StackTraceElement.<init>(String,String,String,int)` plus `getClassName()` / `getMethodName()` / `getLineNumber()` slice used by Nano caller-info helpers, the exact one-argument literal-delimiter `String.split(String)` slice for safe single-character delimiters, the current map-utility slice around `Map.putAll(Map)` and `Collections.emptyMap()`, direct `Enum.valueOf(Class,String)` lowering over the current closed-world enum helpers, `Objects.equals(Object,Object)`, direct `Object.equals(Object)`, `Objects.nonNull(Object)`, byte-array stream `close()` no-op lowering for the current Nano compression/decompression helpers, the current scoped exact `String.replaceAll(String,String)` literal-pair lowering, the current two-entry `Map.of(k1,v1,k2,v2)` slice, `TreeMap.<init>(Map)` and `HashMap.<init>(Map)` over the current generic map runtime, `instanceof java/util/Optional` over the current optional runtime kind, the current `AtomicLong` constructor/get/increment/decrement slice plus the atomic runtime `instanceof` continuation around `AtomicLong`, `AtomicInteger`, `AtomicBoolean`, `AtomicReference`, and `Number.class.isInstance(...)` / `AtomicLong.class.isInstance(...)`, verifier acceptance plus lowering for `pop2`, the exact-owner `ArrayList` inherited runtime slice around `size()` and `stream()`, the `Collection.isEmpty()` interface-owner slice, the new list-view slice around `Collections.unmodifiableList(List)` plus `ArrayList.reversed()`, the current `Collection.forEach(Consumer)` interface-owner slice, the current throwable stack-trace slice around `Throwable.getStackTrace()` plus `Throwable.setStackTrace(StackTraceElement[])`, the new runtime-backed `String.format(String,Object...)` subset for `%s`, `%d`, width, and `%.0s` patterns used by the live Nano frontier, the current runtime-backed `String.isBlank()`, `Character.toString()`, `instanceof java/lang/String`, and no-op `java.util.logging.Formatter.<init>()V` slice, `LinkedHashSet(Collection)` / `HashSet(Collection)` runtime population, `List.clear()`, the request-side and response-side `HttpExchange` slice (`getRequestURI`, `getRequestMethod`, `getRequestHeaders`, `getRequestBody`, `getResponseHeaders`, `sendResponseHeaders`, `getResponseBody`, `Headers.getFirst`, `Headers.put`, `InputStream.readAllBytes`, `OutputStream.write/flush/close`), the plain-HTTP `HttpServer` slice (`create`, `getAddress`, `setExecutor`, `createContext`, `start`, `stop`, and `HttpHandler` lambda/closed-world dispatch), the exact collector-chain slice for `Collectors.toCollection(Supplier)` and `Collectors.toMap(Function,Function,BinaryOperator,Supplier)`, the void-SAM discarded-return LambdaMetafactory slice for fluent setter / collection-add method references, the time-conversion continuation slice around `Calendar.getInstance()`, `Calendar.setTime(Date)`, `Calendar.set(int,int)`, `Calendar.getTimeInMillis()`, `Calendar.toInstant()`, `LocalTime.getHour/getMinute/getSecond/getNano`, `ZonedDateTime.toOffsetDateTime()`, and `OffsetDateTime.toInstant()`, the temporal-normalization continuation slice around `TemporalAccessor.query/isSupported`, `TemporalQueries.zone/localDate/localTime`, `LocalDate.from/now(ZoneId)`, `LocalTime.from`, `ZonedDateTime.of(...)`, `Calendar.setTimeInMillis(long)`, `LocalTime.MIDNIGHT`, the temporal parse conversion slice around `DateTimeFormatter.parse(...)`, `Instant.from(...)`, `ZonedDateTime.from(...)`, and the exact `DateTimeFormatterBuilder.appendLiteral(C)` continuation that now preserves adjacent parse-pattern fragments as one effective pattern token, plus the current JUL carrier continuation around `Level.intValue()/toString()`, cached `Level` singletons, synthetic `LogRecord`, `Formatter.formatMessage(LogRecord)`, the strict `Long.parseLong(String)` slice, the exact `SimpleDateFormat` Nano logging seam, the UUID printable-object seam used by Nano thread naming, the Nano metric stream frontier slice around `Stream.of([Object])` plus `IntStream.range/filter/findFirst`, the zoned-offset time frontier slice around `ZonedDateTime.now()/now(ZoneId)/getOffset()` plus `ZoneOffset.getTotalSeconds()`, the MetricCache frontier slice around `Map.Entry.comparingByKey()` plus `Stream.reduce(BinaryOperator)`, the current port-only `InetSocketAddress.<init>(I)V` slice used by Nano HTTPS helper setup, and the M122 management/concurrency continuation around `ThreadInfo.getLockName()/getLockOwnerName()`, `ThreadMXBean.getAllThreadIds()/getThreadInfo(long[])`, `ExecutorService.submit(Runnable)`, `Future.cancel(boolean)`, and the exact `CountDownLatch` starter slice. The last measured stripped route is still `441` total diagnostics (`427` errors, `14` warnings). | Exact collector-chain support remains intentionally exact-only; the next reachable gates now start at scheduler lifecycle seams such as `ScheduledExecutorService.isShutdown()/isTerminated()`, broader executor surfaces such as `ExecutorService.submit(Callable)`, broader `invokedynamic`, broader `DateTimeFormatterBuilder` value/zone formatter shapes, and higher-level HTTP/TLS/server seams such as `HttpClient$Version.toString()` and `HttpsServer.create(...)` before Nano HTTP runtime behavior can be claimed. |

Current compatibility probes:

- TypeMap: `src/test/resources/projects/real-probes/typemap-pair` builds against the pinned Maven-cache TypeMap jar by default and prints `value`.
- Nano: `src/test/resources/projects/real-probes/nano-metric` builds against the pinned Maven-cache Nano jar by default and prints `requests`.
- Nano duration example slice: `src/test/resources/projects/real-probes/nano-duration` builds against the pinned Maven-cache
  Nano jar by default and prints `1m 5s` using `NanoUtils.formatDuration(long)`, the helper
  used by the upstream example's `/load1` response path. `DevConsoleService` is not
  included.

`.github/scripts/acceptance.sh` now auto-discovers these pinned Maven artifacts and CI prefetches
them before `mvn verify`, so the three probes are required in the acceptance gate. Local runs still
skip cleanly when the artifacts are absent.

These probes prove that the backend can consume real dependency bytecode for simple object constructors, object fields, object returns, object arrays, records, scalar long/float/double operations, primitive arrays, basic enum names, closed-world virtual/interface dispatch, static fields, reachable class initializers, javac string concatenation, basic string intrinsics, selected Nano static helper code, direct same-method exception catches, uncaught panic-style exceptions, and concrete instance calls.

Known blockers before broader TypeMap/Nano coverage:

- only the current blocking TCP loopback socket slice is implemented
- only raw loopback HTTP responder slices are verified so far (`GET` success, unmatched-route `404`, POST body handling, sequential two-connection lifetime, method-plus-path dispatch, multi-class route-handler dispatch, request-header matching, response-header emission, and a request/response object model over router and service classes); no higher-level native HTTP server API yet
- stripped real Nano `/hello` service route now also gets past the inherited default-interface regression on `TypeListI.addR(...)` / `TypeListI.addAllR(...)`, the direct `String.replace(CharSequence,CharSequence)` seam, the focused `Class.isInstance(Object)` / `Class.cast(Object)` / `Class.isEnum()` / `Class.isAssignableFrom(Class)` slice with user-defined subclass parity, the current `Class.getCanonicalName()` alias-on-runtime-class-name slice, the end-to-end `StackTraceElement.<init>(String,String,String,int)` plus `getClassName()` / `getMethodName()` / `getLineNumber()` slice, the exact one-argument literal-delimiter `String.split(String)` slice for safe single-character delimiters, the current `Map.putAll(Map)` plus `Collections.emptyMap()` seam, direct `Enum.valueOf(Class,String)` lowering, `Objects.equals(Object,Object)`, `Objects.nonNull(Object)`, byte-array stream `close()` no-op lowering, the current scoped exact `String.replaceAll(String,String)` literal-pair slice, the current two-entry `Map.of(k1,v1,k2,v2)` slice, `TreeMap.<init>(Map)`, `HashMap.<init>(Map)`, `instanceof java/util/Optional`, the current `AtomicLong` constructor/get/increment/decrement slice plus the atomic runtime `instanceof` continuation around `AtomicLong`, `AtomicInteger`, `AtomicBoolean`, `AtomicReference`, and `Number.class.isInstance(...)` / `AtomicLong.class.isInstance(...)`, verifier acceptance plus lowering for `pop2`, the current exact-owner `ArrayList` inherited runtime slice, the new list-view slice around `Collections.unmodifiableList(List)` plus `ArrayList.reversed()`, the current `Collection.forEach(Consumer)` slice, the current throwable stack-trace slice, the runtime-backed `String.format(...)` subset, the current `String.isBlank()` / `Character.toString()` / `instanceof java/lang/String` / `Formatter.<init>()V` slice, `LinkedHashSet(Collection)` / `HashSet(Collection)` runtime population, `List.clear()`, the request-side and response-side `HttpExchange` slice (`getRequestURI`, `getRequestMethod`, `getRequestHeaders`, `getRequestBody`, `getResponseHeaders`, `sendResponseHeaders`, `getResponseBody`, `Headers.getFirst`, `Headers.put`, `InputStream.readAllBytes`, `OutputStream.write/flush/close`), the plain-HTTP `HttpServer` slice (`create`, `getAddress`, `setExecutor`, `createContext`, `start`, `stop`, and `HttpHandler` lambda/closed-world dispatch), the exact collector-chain slice for `Collectors.toCollection(Supplier)` and `Collectors.toMap(Function,Function,BinaryOperator,Supplier)`, the void-SAM discarded-return LambdaMetafactory slice for fluent setter / collection-add method references, the current time-conversion continuation slice around `Calendar.getInstance()`, `Calendar.setTime(Date)`, `Calendar.set(int,int)`, `Calendar.getTimeInMillis()`, `Calendar.toInstant()`, `LocalTime.getHour/getMinute/getSecond/getNano`, `ZonedDateTime.toOffsetDateTime()`, and `OffsetDateTime.toInstant()`, the temporal-normalization continuation slice around `TemporalAccessor.query/isSupported`, `TemporalQueries.zone/localDate/localTime`, `LocalDate.from/now(ZoneId)`, `LocalTime.from`, `ZonedDateTime.of(...)`, `Calendar.setTimeInMillis(long)`, and `LocalTime.MIDNIGHT`, the temporal parse conversion slice around `DateTimeFormatter.parse(...)`, `Instant.from(...)`, `ZonedDateTime.from(...)`, and the exact `DateTimeFormatterBuilder.appendLiteral(C)` continuation that now preserves adjacent parse-pattern fragments as one effective pattern token, the JUL carrier continuation around `Level.intValue()/toString()`, cached `Level` singletons, synthetic `LogRecord`, `Formatter.formatMessage(LogRecord)`, the strict `Long.parseLong(String)` slice, the exact `SimpleDateFormat` Nano logging seam, the UUID printable-object seam, the Nano metric stream frontier slice around `Stream.of([Object])` plus `IntStream.range/filter/findFirst`, the zoned-offset time frontier slice around `ZonedDateTime.now()/now(ZoneId)/getOffset()` plus `ZoneOffset.getTotalSeconds()`, the MetricCache frontier slice around `Map.Entry.comparingByKey()` plus `Stream.reduce(BinaryOperator)`, the current port-only `InetSocketAddress.<init>(I)V` slice, and the M122 management/concurrency continuation around `ThreadInfo.getLockName()/getLockOwnerName()`, `ThreadMXBean.getAllThreadIds()/getThreadInfo(long[])`, `ExecutorService.submit(Runnable)`, `Future.cancel(boolean)`, and the exact `CountDownLatch` starter slice; its last measured deterministic blocker set is still `441` total diagnostics (`427` errors, `14` warnings). Known remaining gates now start at scheduler lifecycle seams such as `ScheduledExecutorService.isShutdown()/isTerminated()`, broader executor surfaces such as `ExecutorService.submit(Callable)`, broader `invokedynamic`, broader `DateTimeFormatterBuilder` value/zone formatter shapes, higher-level HTTP/TLS/server seams such as `HttpClient$Version.toString()`, `HttpsServer.create(...)`, and broader management/network reachability
- no native HTTPS/TLS runtime yet
- no certificate/trust-store model yet
- no thread-root model for network service lifetimes yet
- richer class initialization ordering across complex dependency graphs
- full enum initialization beyond basic constant names
- `invokedynamic` lambdas and dynamic-call sites
- non-ASCII/full UTF-16 `String` runtime semantics
- broader try/catch/finally exception-handler lowering beyond the current exact try-with-resources and shared null-return clusters
- common JDK intrinsics for `String`, collections, streams, broader `Optional`, and broader atomic families

Fresh Nano packaging may still fail if a broader Nano graph resolves a TypeMap version
that does not provide `JsonDecoder.jsonTypeOf(String)`. The `src/test/resources/projects/real-probes/nano-metric` probe
still accepts `NANO_CLASSES=/path/to/nano/target/classes` as a local fallback, but the release gate now uses the pinned
published Nano jar rather than a sibling checkout.

Next gates before claiming Nano support:

1. done: make the three real probes reproducible and required in at least one CI row
2. done: add negative diagnostics for `Socket`, `ServerSocket`, `HttpClient`, and Nano-style `HttpServer`
3. done: report reachable `network`, `socket`, and `http` usage even while unsupported
4. implement TCP loopback support with close/ownership and sanitizer proof
5. done partially: implement plain HTTP client loopback support for GET/string, POST+headers/byte[], and PUT byte[], plus raw loopback responder slices over `ServerSocket`/`Socket` for `GET /hello -> 200 pong`, unmatched-route `404`, POST body handling via `Content-Length`, sequential two-connection lifetime, method-plus-path dispatch, multi-class route-handler dispatch, request-header matching, response-header emission, and a request/response object model over router and service classes
6. in progress: stripped Nano `/hello` route without `DevConsoleService` still measures `441` total diagnostics (`427` errors, `14` warnings), and the inherited `TypeListI.addR(...)` / `TypeListI.addAllR(...)` default-interface dispatch, the direct `String.replace(CharSequence,CharSequence)` seam, the focused class-runtime slice (`Class.isInstance`, `Class.cast`, `Class.isEnum`, `Class.isAssignableFrom` with user-defined subclass parity and explicit enum-instance rejection), the current `Class.getCanonicalName()` alias-on-runtime-class-name slice, the end-to-end `StackTraceElement.<init>(String,String,String,int)` plus `getClassName()` / `getMethodName()` / `getLineNumber()` slice, the current throwable stack-trace slice, the exact one-argument literal-delimiter `String.split(String)` slice for safe single-character delimiters, the current `Map.putAll(Map)` / `Collections.emptyMap()` seam, direct `Enum.valueOf(Class,String)` lowering, `Objects.equals(Object,Object)`, `Objects.nonNull(Object)`, byte-array stream `close()` no-op lowering, the current scoped exact `String.replaceAll(String,String)` literal-pair slice, the current two-entry `Map.of(k1,v1,k2,v2)` slice, `TreeMap.<init>(Map)`, `HashMap.<init>(Map)`, `instanceof java/util/Optional`, the current `AtomicLong` constructor/get/increment/decrement slice plus the atomic runtime `instanceof` continuation around `AtomicLong`, `AtomicInteger`, `AtomicBoolean`, `AtomicReference`, and `Number.class.isInstance(...)` / `AtomicLong.class.isInstance(...)`, verifier acceptance plus lowering for `pop2`, the current exact-owner `ArrayList` inherited runtime slice, the new list-view slice around `Collections.unmodifiableList(List)` plus `ArrayList.reversed()`, the current `Collection.forEach(Consumer)` slice, the runtime-backed `String.format(...)` subset, the exact collector-chain slice for `Collectors.toCollection(Supplier)` and `Collectors.toMap(Function,Function,BinaryOperator,Supplier)`, the void-SAM discarded-return LambdaMetafactory slice for fluent setter / collection-add method references, the current time-conversion continuation slice around `Calendar.getInstance()`, `Calendar.setTime(Date)`, `Calendar.set(int,int)`, `Calendar.getTimeInMillis()`, `Calendar.toInstant()`, `LocalTime.getHour/getMinute/getSecond/getNano`, `ZonedDateTime.toOffsetDateTime()`, and `OffsetDateTime.toInstant()`, the temporal-normalization continuation slice around `TemporalAccessor.query/isSupported`, `TemporalQueries.zone/localDate/localTime`, `LocalDate.from/now(ZoneId)`, `LocalTime.from`, `ZonedDateTime.of(...)`, `Calendar.setTimeInMillis(long)`, and `LocalTime.MIDNIGHT`, the temporal parse conversion slice around `DateTimeFormatter.parse`, `Instant.from`, `ZonedDateTime.from`, and the exact `DateTimeFormatterBuilder.appendLiteral(C)` continuation that now preserves adjacent parse-pattern fragments as one effective pattern token, the JUL carrier continuation around `Level.intValue()/toString()`, cached `Level` singletons, synthetic `LogRecord`, `Formatter.formatMessage(LogRecord)`, the strict `Long.parseLong(String)` slice, the exact `SimpleDateFormat` Nano logging seam, the UUID printable-object seam, the Nano metric stream frontier slice around `Stream.of([Object])` plus `IntStream.range/filter/findFirst`, the zoned-offset time frontier slice around `ZonedDateTime.now()/now(ZoneId)/getOffset()` plus `ZoneOffset.getTotalSeconds()`, the MetricCache frontier slice around `Map.Entry.comparingByKey()` plus `Stream.reduce(BinaryOperator)`, the current port-only `InetSocketAddress.<init>(I)V` slice, the response-side `HttpExchange` slice, and the M122 management/concurrency continuation around `ThreadInfo.getLockName()/getLockOwnerName()`, `ThreadMXBean.getAllThreadIds()/getThreadInfo(long[])`, `ExecutorService.submit(Runnable)`, `Future.cancel(boolean)`, and the exact `CountDownLatch` starter slice are fixed locally. The next pinned gates are scheduler lifecycle seams such as `ScheduledExecutorService.isShutdown()/isTerminated()`, broader executor surfaces such as `ExecutorService.submit(Callable)`, general `invokedynamic` sites deeper in `TypeConversionRegister` / `Nano` core flows, broader `DateTimeFormatterBuilder` value/zone formatter shapes, higher-level HTTP/TLS/server seams such as `HttpClient$Version.toString()`, `HttpServer.create(...)`, `HttpsServer.create(...)`, and broader management/network runtime support before the HTTP runtime
7. add HTTPS/TLS/certificates after plain HTTP is stable
