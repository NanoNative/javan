# Real Project Readiness

Status summary:

| Project | Status | Current evidence | Missing release gate |
| --- | --- | --- | --- |
| TypeMap | Partial | `typemap-pair` builds natively against the pinned Maven artifact and is exercised by required CI acceptance plus focused CLI integration. | Broader TypeMap dependency graph and quieter unreachable-dependency diagnostics. |
| Nano metrics helper | Partial | `nano-metric` builds natively against the pinned Maven artifact and is exercised by required CI acceptance plus focused CLI integration. | Broader Nano dependency graph and quieter unreachable-dependency diagnostics. |
| Nano duration helper | Partial | `nano-duration` builds natively against the pinned Maven artifact and is exercised by required CI acceptance plus focused CLI integration. | Broader Nano helper surface and quieter unreachable-dependency diagnostics. |
| Nano HTTP service | In progress | A stripped real Nano `/hello` service route without `DevConsoleService` now gets past inherited `Nano.subscribeEvent(...)->NanoBase` resolution, inherited JDK `Context.computeIfAbsent(..., Function)` classification, inherited TypeMap default-interface false positives, inherited concrete-owner `invokevirtual` false positives on TypeMap default methods such as `Type.isPresent(...)` / `Type.or(...)`, the hidden-lambda default wrapper false positive on `FunctionOrNull.apply(Object)`, the inherited-JDK `Context.put(...)->ConcurrentTypeMap.put(...)` false positive, the exact captured `LambdaMetafactory.metafactory` closure shapes behind the previous `FunctionOrNull.applyWithException(Object)` and `ExRunnable.run()` blockers, the direct `Optional.filter` / `Optional.ifPresent` lambda chain used by the route shape, the next higher-order `Optional.map` / `Optional.ifPresentOrElse` / `Optional.orElseGet` JDK seam, the exact closed-world `Predicate.test` / `Function.apply` / `Consumer.accept` / `Supplier.get` / `Runnable.run` / `BiConsumer.accept` / `BooleanSupplier.getAsBoolean` dispatch behind those lambdas, the current `AtomicBoolean` / `AtomicInteger` starter state holders used in the Nano thread-state frontier, the inherited `Map.computeIfAbsent(..., Function)` map seam, the current `CopyOnWriteArrayList` constructor/runtime bridge used in the broader concurrency surface, the current class-metadata seam (`Object.getClass`, `Class.getName`, `Class.getSimpleName`, `Class.isArray`), the eager reference-stream starter slice (`List.stream`, `Collection.stream`, `Stream.filter`, `Stream.map`, `Stream.toList`, `Stream.findFirst`, `Stream.anyMatch`, `Stream.noneMatch`, `Stream.forEach`), the unbound LambdaMetafactory bridge slice for method references like `Supplier::get` / `BooleanSupplier::getAsBoolean`, the `Arrays.stream(Object[])` source slice, the `l2d` opcode seam, the exact direct `Collectors.joining(...) -> Stream.collect(...)` slice, the current `Map.entrySet()` / `Set.stream()` / `Map.Entry.getKey()/getValue()` slice, the current `Map.forEach(...)` slice, the current platform-throwable suppressed-exception slice, the current ASCII string-case / boxed-character slice (`String.equalsIgnoreCase(String)`, `String.toLowerCase()`, `Character.valueOf(char)`, `Character.charValue()`), the exact javac three-handler try-with-resources suppressed-exception cluster for cleanup methods that only construct and throw a known platform throwable, the exact javac shared typed null-return handler cluster where multiple protected ranges feed one platform-exception handler whose body is `astore; aconst_null; areturn`, the boxed-`Number` starter slice (`instanceof Number`, `Number.intValue()`), the TypeMap `DateTimeFormatterBuilder` bootstrap cluster, user-enum `Enum.valueOf(String)` lowering, the concurrent-set starter slice around `ConcurrentHashMap.newKeySet()` plus `Set` / `KeySetView` add-remove-size-toArray flows, the map-mutation slice around `Map.remove(Object)` / `Map.clear()`, the list-utility slice around `Collections.emptyList()` plus `List.remove(Object)` / `List.forEach(Consumer)`, the concurrent-map exact-owner starter slice around `ConcurrentHashMap.<init>()V` plus the current exact-owner non-callback map methods, the current temporal conversion starter slice around `ZoneId`, `Instant`, `Date`, `LocalDate`, `LocalDateTime`, and `ZonedDateTime`, and the current eager int-stream max slice around `Stream.mapToInt(ToIntFunction)`, `IntStream.max()`, `OptionalInt.orElse(int)`, and direct `ToIntFunction.applyAsInt(Object)` dispatch. The stripped route now fails deeper in the broader Nano graph at `928` total diagnostics, with the current first blockers centered on broader `JAVAN031` reachability gaps, broader `JAVAN030` `invokedynamic`, broader exception-handler lowering beyond the current exact try-with-resources and shared null-return clusters, and management/runtime hooks such as `com/sun/management/OperatingSystemMXBean.getProcessCpuLoad()/getCpuLoad()` and `java/lang/Runtime.getRuntime()/addShutdownHook(Thread)`. | Broader lambda `invokedynamic`, broader reachability gaps, broader exception-handler lowering beyond the current exact try-with-resources and shared null-return clusters, management/runtime hooks, and wider stream/concurrency APIs before HTTP runtime behavior can be claimed. |

Current compatibility probes:

- TypeMap: `src/test/resources/projects/real-probes/typemap-pair` builds against the pinned Maven-cache TypeMap jar by default and prints `value`.
- Nano: `src/test/resources/projects/real-probes/nano-metric` builds against the pinned Maven-cache Nano jar by default and prints `requests`.
- Nano duration example slice: `src/test/resources/projects/real-probes/nano-duration` builds against the pinned Maven-cache
  Nano jar by default and prints `1m 5s` using `NanoUtils.formatDuration(long)`, the helper
  used by the upstream example's `/load1` response path. `DevConsoleService` is not
  included.

`.github/scripts/acceptance.sh` now auto-discovers these pinned Maven artifacts and CI prefetches
them before `mvn verify`, so the three probes are required in the acceptance gate. Local runs still
skip cleanly when the artifacts are absent.

These probes prove that the backend can consume real dependency bytecode for simple object constructors, object fields, object returns, object arrays, records, scalar long/float/double operations, primitive arrays, basic enum names, closed-world virtual/interface dispatch, static fields, reachable class initializers, javac string concatenation, basic string intrinsics, selected Nano static helper code, direct same-method exception catches, uncaught panic-style exceptions, and concrete instance calls.

Known blockers before broader TypeMap/Nano coverage:

- only the current blocking TCP loopback socket slice is implemented
- only raw loopback HTTP responder slices are verified so far (`GET` success, unmatched-route `404`, POST body handling, sequential two-connection lifetime, method-plus-path dispatch, multi-class route-handler dispatch, request-header matching, response-header emission, and a request/response object model over router and service classes); no higher-level native HTTP server API yet
- stripped real Nano `/hello` service route now gets past inherited `Nano.subscribeEvent(...)->NanoBase` resolution, inherited JDK `Context.computeIfAbsent(..., Function)` classification, inherited TypeMap default-interface false positives, the hidden-lambda default wrapper false positive on `FunctionOrNull.apply(Object)`, the inherited-JDK `Context.put(...)->ConcurrentTypeMap.put(...)` false positive, the previous exact captured-lambda blockers on `FunctionOrNull.applyWithException(Object)` and `ExRunnable.run()`, the direct `Optional.filter` / `Optional.ifPresent` route chain, the next higher-order `Optional.map` / `Optional.ifPresentOrElse` / `Optional.orElseGet` seam, the direct closed-world `Predicate.test` / `Function.apply` / `Consumer.accept` / `Supplier.get` / `Runnable.run` / `BiConsumer.accept` / `BooleanSupplier.getAsBoolean` lambda dispatch, the current `AtomicBoolean` / `AtomicInteger` starter state holders, the inherited `Map.computeIfAbsent(..., Function)` seam, the current `CopyOnWriteArrayList` constructor/runtime bridge, the current class-metadata seam, the eager reference-stream starter slice plus terminal `Stream.forEach(Consumer)` slice, the current unbound LambdaMetafactory bridge slice, the `Arrays.stream(Object[])` source slice, the `l2d` opcode seam, the exact direct `Collectors.joining(...) -> Stream.collect(...)` slice, the current `Map.entrySet()` / `Set.stream()` / `Map.Entry.getKey()/getValue()` slice, the current `Map.forEach(...)` slice, the current platform-throwable suppressed-exception slice, the current ASCII string-case / boxed-character slice, the exact javac three-handler try-with-resources suppressed-exception cluster for cleanup methods that only construct and throw a known platform throwable, the exact javac shared typed null-return handler cluster where multiple protected ranges feed one platform-exception handler whose body is `astore; aconst_null; areturn`, the boxed-`Number` starter slice (`instanceof Number`, `Number.intValue()`), the TypeMap `DateTimeFormatterBuilder` bootstrap cluster, user-enum `Enum.valueOf(String)` lowering, the concurrent-set starter slice around `ConcurrentHashMap.newKeySet()` plus `Set` / `KeySetView` add-remove-size-toArray flows, the map-mutation slice around `Map.remove(Object)` / `Map.clear()`, the list-utility slice around `Collections.emptyList()` plus `List.remove(Object)` / `List.forEach(Consumer)`, the concurrent-map exact-owner starter slice around `ConcurrentHashMap.<init>()V` plus the current exact-owner non-callback map methods, the temporal conversion starter slice around `ZoneId`, `Instant`, `Date`, `LocalDate`, `LocalDateTime`, and `ZonedDateTime`, and the current eager int-stream max slice around `Stream.mapToInt(ToIntFunction)`, `IntStream.max()`, `OptionalInt.orElse(int)`, and direct `ToIntFunction.applyAsInt(Object)` dispatch; its current deterministic blocker set is now pinned deeper at `928` total diagnostics as broader lambda `invokedynamic`, broader reachability gaps, broader exception-handler lowering beyond the current exact try-with-resources and shared null-return clusters, management/runtime hooks, and wider stream/concurrency support before HTTP runtime behavior can be claimed
- no native HTTPS/TLS runtime yet
- no certificate/trust-store model yet
- no thread-root model for network service lifetimes yet
- richer class initialization ordering across complex dependency graphs
- full enum initialization beyond basic constant names
- `invokedynamic` lambdas and dynamic-call sites
- non-ASCII/full UTF-16 `String` runtime semantics
- broader try/catch/finally exception-handler lowering beyond the current exact try-with-resources and shared null-return clusters
- common JDK intrinsics for `String`, collections, streams, broader `Optional`, and broader atomic families

Fresh Nano packaging may still fail if a broader Nano graph resolves a TypeMap version
that does not provide `JsonDecoder.jsonTypeOf(String)`. The `src/test/resources/projects/real-probes/nano-metric` probe
still accepts `NANO_CLASSES=/path/to/nano/target/classes` as a local fallback, but the release gate now uses the pinned
published Nano jar rather than a sibling checkout.

Next gates before claiming Nano support:

1. done: make the three real probes reproducible and required in at least one CI row
2. done: add negative diagnostics for `Socket`, `ServerSocket`, `HttpClient`, and Nano-style `HttpServer`
3. done: report reachable `network`, `socket`, and `http` usage even while unsupported
4. implement TCP loopback support with close/ownership and sanitizer proof
5. done partially: implement plain HTTP client loopback support for GET/string, POST+headers/byte[], and PUT byte[], plus raw loopback responder slices over `ServerSocket`/`Socket` for `GET /hello -> 200 pong`, unmatched-route `404`, POST body handling via `Content-Length`, sequential two-connection lifetime, method-plus-path dispatch, multi-class route-handler dispatch, request-header matching, response-header emission, and a request/response object model over router and service classes
6. in progress: stripped Nano `/hello` route without `DevConsoleService` now reproduces the current blocker set deterministically; inherited `Nano.subscribeEvent(...)->NanoBase` resolution, inherited JDK `Context.computeIfAbsent(..., Function)` classification, inherited TypeMap default-interface false positives, the hidden-lambda default wrapper false positive on `FunctionOrNull.apply(Object)`, the inherited-JDK `Context.put(...)->ConcurrentTypeMap.put(...)` false positive, the previous exact captured-lambda blockers on `FunctionOrNull.applyWithException(Object)` and `ExRunnable.run()`, the direct `Optional.filter` / `Optional.ifPresent` route chain, the next higher-order `Optional.map` / `Optional.ifPresentOrElse` / `Optional.orElseGet` seam, the direct closed-world `Predicate.test` / `Function.apply` / `Consumer.accept` / `Supplier.get` / `Runnable.run` / `BiConsumer.accept` / `BooleanSupplier.getAsBoolean` dispatch, the current `AtomicBoolean` / `AtomicInteger` starter state holders, the inherited `Map.computeIfAbsent(..., Function)` seam, the current `CopyOnWriteArrayList` constructor/runtime bridge, the current class-metadata seam, the eager reference-stream starter slice plus terminal `Stream.forEach(Consumer)` slice, the current unbound LambdaMetafactory bridge slice, the `Arrays.stream(Object[])` source slice, the `l2d` opcode seam, the exact direct `Collectors.joining(...) -> Stream.collect(...)` slice, the current `Map.entrySet()` / `Set.stream()` / `Map.Entry.getKey()/getValue()` slice, the current `Map.forEach(...)` slice, the current platform-throwable suppressed-exception slice, the current ASCII string-case / boxed-character slice, the exact javac three-handler try-with-resources suppressed-exception cluster for cleanup methods that only construct and throw a known platform throwable, the exact javac shared typed null-return handler cluster where multiple protected ranges feed one platform-exception handler whose body is `astore; aconst_null; areturn`, the boxed-`Number` starter slice (`instanceof Number`, `Number.intValue()`), the TypeMap `DateTimeFormatterBuilder` bootstrap cluster, user-enum `Enum.valueOf(String)` lowering, the concurrent-set starter slice around `ConcurrentHashMap.newKeySet()` plus `Set` / `KeySetView` add-remove-size-toArray flows, the map-mutation slice around `Map.remove(Object)` / `Map.clear()`, the list-utility slice around `Collections.emptyList()` plus `List.remove(Object)` / `List.forEach(Consumer)`, the concurrent-map exact-owner starter slice around `ConcurrentHashMap.<init>()V` plus the current exact-owner non-callback map methods, and the temporal conversion starter slice around `ZoneId`, `Instant`, `Date`, `LocalDate`, `LocalDateTime`, and `ZonedDateTime` are fixed locally, and the current pinned gates are broader lambda `invokedynamic`, broader reachability gaps, broader exception-handler lowering beyond the current exact try-with-resources and shared null-return clusters, management/runtime hooks, and wider stream/concurrency support before the HTTP runtime
7. add HTTPS/TLS/certificates after plain HTTP is stable
